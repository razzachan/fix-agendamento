# Webhook-AI - Exemplo de configuração (.env)
# Duplique este arquivo como .env e ajuste os valores

# Porta HTTP do serviço
PORT=3100

# Supabase (necessário para sessões e notificações internas)
SUPABASE_URL=https://SEU-PROJETO.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sbp_..._service_role

# OpenAI (se usar IA para roteamento/humanização)
OPENAI_API_KEY=sk-...

# Handoff humano
# Prioridade 1: usa o UUID do usuário no Auth (auth.users.id)
# Descobrimos o ID para admin@fixfogoes.com.br: a1c05f82-f9fb-42b8-89ff-540046f61b80
# Em produção, você pode manter este valor ou trocar pelo ID do admin do seu ambiente
FIX_HANDOFF_ADMIN_ID=a1c05f82-f9fb-42b8-89ff-540046f61b80

# Prioridade 2 (fallback): e-mail do admin (deve existir em public.users com role=admin)
FIX_HANDOFF_EMAIL=admin@fixfogoes.com.br

# Chave para acessar o endpoint admin-only /admin/health
# Envie no header: x-admin-key: ${ADMIN_API_KEY}
ADMIN_API_KEY=defina-um-segredo-forte
# (Troque este valor em produção por um segredo forte e diferente por ambiente)

# URLs para o script de health admin (opcional)
# ADMIN_HEALTH_URL=http://localhost:3100
# ADMIN_HEALTH_URL_PROD=https://sua-url-de-producao


# Seed de admin no boot (apenas cria admin padrão se nenhum existir)
# Para desativar em algum ambiente:
# ADMIN_SEED_DISABLE=true

